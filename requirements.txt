# Dependencies — listed alphabetically, one purpose note each.
# GPU build of TensorFlow assumed; use plain `tensorflow` on a CPU-only box.
kagglehub              # CICIDS 2017 download (Part 2.2)
matplotlib             # all plotting
medmnist               # OCTMNIST dataset (Part 2.1)
numpy                  # arrays / numerics
pandas                 # CICIDS CSV handling
pillow                 # image I/O
requests               # QuickDraw bitmap fetch
scikit-learn           # PCA, t-SNE, scalers (Part 2.2)
scipy                  # matrix sqrt for FID
tensorflow[and-cuda]   # Parts 2.1 and 2.2
torch                  # Parts 1 and 2.3
torchvision            # InceptionV3 features for FID (Part 2.3)
tqdm                   # progress bars
